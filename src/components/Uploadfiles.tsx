import { DocumentData } from "firebase/firestore"
import folders from "../types/folder"


const Uploadfiles = ({folders}:{folders:Array<folders|DocumentData>}) => {
  return (
    <div>Uploadfiles</div>
  )
}

export default Uploadfiles